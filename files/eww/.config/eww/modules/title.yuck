
(deflisten win_title :initial "..." "./scripts/win_title.sh")

(defwidget title []
    (box
        (label
            :class "win-title"
            :truncate true
            :limit-width 60
            :text "${win_title}"
        )
    )
)
