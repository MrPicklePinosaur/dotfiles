
# unbind stuff
bind index,pager gg noop
bind index,pager gk noop
bind index,pager gj noop
bind index,pager g noop
bind index,pager b noop
bind index,pager R noop

# navigation
bind index gg first-entry
bind index G last-entry
bind pager j next-line
bind pager k previous-line
bind index,pager,browser \Cd half-down
bind index,pager,browser \Cu half-up

# sidebar
bind index,pager b sidebar-toggle-visible
macro index,pager gk <sidebar-prev><sidebar-open>
macro index,pager gj <sidebar-next><sidebar-open>

# reload mutt
# TODO send message in command line to indicate the mutt was reloaded
macro index,pager R "<enter-command>source .config/mutt/muttrc<enter>"

